server:
  port: 8080
spring:
  application:
    name: pagamentos
  jpa:
  hibernate:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: 'jdbc:postgresql://localhost:5432/alurafood-pagamento'
    username: postgres
    password: postgres